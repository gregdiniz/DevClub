/*
Sempre que for necessário realizar uma referência ao documento HTML usamo a expressão 'document'

querySelector() -> Traz TODOS os elementos que encotrar
*/

const qsaTag = document.querySelectorAll("input")

console.log(qsaTag)